import React from 'react';

// This component is intentionally left empty as Watermark feature has been removed.
// Keeping file to prevent import errors if cached, but rendering nothing.
export const WatermarkOverlay: React.FC = () => {
  return null;
};